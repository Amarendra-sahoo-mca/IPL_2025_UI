export enum player_Role {
    BATSMAN = 1,
    BOWLER = 2,
    ALL_ROUNDER = 3
}

export enum player_status {
    WRITTEN = 1,
    BUY = 2
}
export enum Sorting {
    ASC = 'ASC',
    DESC = 'DESC',
}